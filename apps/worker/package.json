{
  "name": "@lead-finder/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b --pretty false",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@lead-finder/batch-processor": "*",
    "@lead-finder/database": "*",
    "@lead-finder/enrichment": "*",
    "@lead-finder/lead-scoring": "*",
    "@lead-finder/overpass-client": "*",
    "@lead-finder/shared": "*"
  }
}
